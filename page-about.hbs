{{!< default}}

              {{! This is a page template. A page outputs content just like any other post, and has all the same
              attributes by default, but you can also customise it to behave differently if you prefer. }}

              {{! Everything inside the #post tags pulls data from the page }}
              {{#post}}

              <header class="main-header post-head {{#if image}}" style="background-image: url({{image}}){{else}}no-cover{{/if}}">
<nav class="main-nav {{#if image}}overlay{{/if}} clearfix">
    {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="Blog Logo" /></a>{{/if}}
    {{!--{{#if @blog.navigation}}
    <a class="menu-button icon-menu" href="#"><span class="word">Menu</span></a>
    {{/if}}--}}
</nav>
</header>

<header class="post-header container">
    <h1 class="post-title">{{title}}</h1>
</header>

<main class="content" role="main">
    <article class="post page">
        <section class="post-content">
            <div class="background-scroller" style="background-image: url({{@blog.cover}})"></div>
            <div class="filter-scroller"></div>

            <div class="container page-container">
                {{content}}
            </div>

        </section>
    </article>
</main>
<div class="rollover-section">
    <div class="container">
        <h1 style="text-align:center">We’ll probably often be wrong, or, misdirected, just by nature of learning all this stuff too, as we go. Call us out, call us hypocrites, just call us (well, okay, maybe just email us)–really! We’re not experts, but we do have opinions, and we want to hear yours.</h1>
    </div>
</div>

{{/post}}
